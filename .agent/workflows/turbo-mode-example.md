---
description: Example workflow that runs commands without approval
---

This is an example of a workflow that uses the `// turbo-all` annotation to run all commands automatically.

// turbo-all

1. Echo a hello message
   ```bash
   echo "Hello from Turbo Mode!"
   ```

2. List files in the current directory
   ```bash
   ls
   ```
